{ ... }:

{
  # following configuration is added only when building VM
  virtualisation.vmVariant = {
    virtualisation = {
      memorySize = 8192;
      cores = 4;
      resolution = { x = 1600; y = 900; };
    };
  };
}
